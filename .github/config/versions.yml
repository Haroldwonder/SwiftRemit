# Centralized toolchain versions
# Reference these versions in workflows to avoid duplication

versions:
  node: '20'
  rust: 'stable'
  postgres: '15'
  postgres_alpine: '15-alpine'
  helm: '3.17.0'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
